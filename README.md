# My nvim config

```bash
rm -rf ~/.config/nvim
git clone https://github.com/arthurazs/nvim --branch feature/improve-config-v2 --depth 1 ~/.config/nvim

cd ~/.config/nvim
uv venv
uv pip install pynvim
```
